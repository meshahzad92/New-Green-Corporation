# ============================================
# AgriManage Pro - Environment Configuration
# ============================================

# Database Configuration - PostgreSQL (Production)
DATABASE_URL=postgresql://agrimanage:agrimanage_password@postgres:5432/agrimanage_db

# PostgreSQL Credentials
POSTGRES_USER=agrimanage
POSTGRES_PASSWORD=agrimanage_password
POSTGRES_DB=agrimanage_db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Security - Generate with: openssl rand -hex 32
SECRET_KEY=CHANGE-THIS-TO-SECURE-RANDOM-KEY-IN-PRODUCTION

# JWT Settings
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Timezone
TZ=Asia/Karachi

# Production Notes:
# 1. Generate SECRET_KEY: openssl rand -hex 32
# 2. Use strong POSTGRES_PASSWORD (16+ characters)
# 3. Restrict CORS if needed in config.py
